<div class="container">
  <h1>Sign in</h1>
  <%= form_for(:session, url: signin_path) do %>
    <div class="form-group">
      <%= label_tag :email %>
      <%= text_field_tag :email %>
    </div>
    <div class="form-group">
      <%= label_tag :password %>
      <%= password_field_tag :password %>
    </div>
    <%= submit_tag "Sign in", :name=> nil, class: "btn btn-primary"%>
  <% end %>

  <p><%= link_to "Forgot Password?", new_password_reset_path %></p>

  <br>
  <p>Decisions are viewable by anyone, regardless of whether they are logged in. To help add decisions, speak to Maria or Rita for access.</p>
</div>
